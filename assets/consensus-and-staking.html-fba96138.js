import{_ as r,W as i,X as o,Y as e,Z as a,$ as d,a1 as s,C as n}from"./framework-a045178c.js";const l={},h=s('<h1 id="consensus-and-staking" tabindex="-1"><a class="header-anchor" href="#consensus-and-staking" aria-hidden="true">#</a> Consensus and Staking</h1><p>BNB Greenfield blockchain uses Proof-of-Stake based on Tendermint-consensus for its own network security. Blocks are created every 2 seconds on the Greenfield chain by a group of validators. BNB will be the staking token on this blockchain.</p><p>There will be a validators set in the genesis state, the staked BNB for these validators would be locked in the BSC side. For the other validators, they should submit a create-validator proposal to become a validator, and if the validator doesn&#39;t behave well, they can be impeached, once the impeach-validator proposal is passed, the validator would be jailed forever.</p><p>It should be mentioned that the validators are separated from the storage provider. The validators are responsible for generating the blocks, challenging the data availability, and cross-chain communication. The storage providers are responsible for storing the data objects, there is no strong binding relationship between them.</p><h2 id="create-validator" tabindex="-1"><a class="header-anchor" href="#create-validator" aria-hidden="true">#</a> Create Validator</h2><p>To become a validator, a create-validator proposal should be submitted and adopted by the majority of the current validators. Meanwhile, in the early stage, only self delegation is allowed, and in the further open delegation can be supported. Here are the steps for becoming a new validator:</p><ul><li><p>Self delegator of the new validator grants the delegate authorization to the gov module account.</p></li><li><p>The new validator should initiate a create-validator proposal.</p></li><li><p>Wait for the current validators to vote on this proposal.</p></li><li><p>Once the proposal is passed, the new validator would be created automatically.</p></li></ul><h2 id="edit-validator" tabindex="-1"><a class="header-anchor" href="#edit-validator" aria-hidden="true">#</a> Edit Validator</h2><p>There are several fields that validators can edit after they have been created. Including description, commission rate, min-self-delegation, relayer address, and relayer bls public key. All these fields can be edited by submitting an edit-validator transaction.</p><h2 id="impeach-validator" tabindex="-1"><a class="header-anchor" href="#impeach-validator" aria-hidden="true">#</a> Impeach Validator</h2><p>If a validator behaves badly, anyone can submit a proposal to impeach the validator, and if the proposal is passed, the validator would be jailed forever. Here are the steps for impeaching a validator:</p><ul><li><p>Initiate an impeach-validator proposal.</p></li><li><p>Wait for the current validators to vote on this proposal.</p></li><li><p>Once the proposal is passed, the validator would be jailed forever automatically, that means it can’t become an active validator anymore.</p></li></ul><h2 id="staking-reward-distribution" tabindex="-1"><a class="header-anchor" href="#staking-reward-distribution" aria-hidden="true">#</a> Staking Reward Distribution</h2><p>In BNB Greenfield blockchain, rewards gained from transaction fees are paid to validators. The fee distribution module fairly distributes the rewards to the validators&#39; constituent delegators.</p><p>Rewards are calculated per period. The period is updated each time a validator&#39;s delegation changes, for example, when the validator receives a new delegation. The rewards for a single validator can then be calculated by taking the total rewards for the period before the delegation started, minus the current total rewards.</p><p>The commission to the validator is paid when the validator is removed or when the validator requests a withdrawal. The commission is calculated and incremented at every <code>BeginBlock</code> operation to update accumulated fee amounts.</p><p>The rewards to a delegator are distributed when the delegation is changed or removed, or a withdrawal is requested. Before rewards are distributed, all slashes to the validator that occurred during the current delegation are applied.</p>',17),c={href:"https://github.com/bnb-chain/greenfield-cosmos-sdk/blob/master/docs/spec/fee_distribution/f1_fee_distr.pdf",target:"_blank",rel:"noopener noreferrer"};function p(u,v){const t=n("ExternalLinkIcon");return i(),o("div",null,[h,e("p",null,[a("The detailed fee distribution rules is "),e("a",c,[a("described here"),d(t)])])])}const f=r(l,[["render",p],["__file","consensus-and-staking.html.vue"]]);export{f as default};
